# Story 5.6: Vista Agenda — Lista Cronológica de Próximos Compromisos

## Status

Done

## Executor Assignment

```yaml
executor: '@dev'
quality_gate: '@qa'
quality_gate_tools:
  - docs/architecture/test-strategy.md
  - docs/architecture/source-tree.md
  - docs/prd/technical-assumptions.md
```

## Story

**As a** usuario que quiere ver sus compromisos futuros,
**I want** ver en la Vista Agenda una lista cronológica de las actividades de los próximos 30 días con su hora, duración y área,
**so that** pueda tener visibilidad rápida de mis compromisos sin navegar vista a vista y planificar mejor mis próximos días.

## Dependencies

- **Requires:** Story 5.5 — Done ✅ (`getActivitiesForYear` provee el array completo, `AgendaView` base funcional en `CalendarClient.tsx`, datos reales ya fluyen hasta la vista)
- **Requires:** Story 5.1 — Done ✅ (`calendar-utils.ts` con `getEventsForDay`, `ICalendarEvent` con `description` y `color`)
- **Blocks:** Story 5.7 (CRUD Eventos)

## Acceptance Criteria

### AC1 — AgendaView muestra datos reales del año

**Given** el usuario está en la Vista Agenda y hay actividades en `steps_activities`,
**When** el componente renderiza,
**Then:**
- La Vista Agenda ya recibe los eventos del año completo desde `page.tsx` (via `getActivitiesForYear`) — sin query adicional
- Muestra las actividades de los próximos 30 días desde la fecha actual (`currentDate`) agrupadas por día
- Cada entrada muestra: hora de inicio (HH:mm), título, y área (`description` del evento = `areaName`) si está disponible
- Cada entrada muestra la duración en minutos (calculada como `end - start` en minutos, formato `Xm` o `Xh Ym`)
- El color de área aparece como borde izquierdo coloreado (ya implementado con `EVENT_COLOR_CLASSES`)
- Si no hay eventos en los próximos 30 días, muestra "No hay eventos en los próximos 30 días"

### AC2 — AgendaView muestra duración de cada evento

**Given** el usuario está en la Vista Agenda,
**When** un evento tiene `start` y `end` definidos,
**Then:**
- La duración se muestra junto a la hora: `HH:mm · Xm` o `HH:mm · Xh Ym`
- Duración < 60 min → `Xm` (ej: `09:00 · 30m`)
- Duración >= 60 min → `Xh Ym` (ej: `09:00 · 1h 30m`)
- Reutiliza `formatMinutes()` exportada de `CalendarClient.tsx` (sin duplicar lógica)

### AC3 — AgendaView muestra el área del evento

**Given** el usuario está en la Vista Agenda y un evento tiene `description` (= `areaName`),
**When** el componente renderiza esa fila,
**Then:**
- El nombre del área aparece como badge/tag pequeño junto al título
- Si `description` es `undefined` (no aplica), no muestra nada adicional (sin "undefined" visible)
- El tag usa el mismo color de borde que la fila del evento

### AC4 — Navegación en Vista Agenda avanza/retrocede por meses

**Given** el usuario está en la Vista Agenda y usa los botones de navegación,
**When** presiona "siguiente" o "anterior",
**Then:**
- La vista avanza/retrocede 1 mes (ya implementado: `addMonths`/`subMonths` en `navigate`)
- Muestra los próximos 30 días desde la nueva `currentDate` — sin nueva query (filtra del array del año)
- Al presionar "Hoy" regresa al día actual

### AC5 — Tests: comportamiento de AgendaView

**Given** la implementación completa,
**When** se ejecutan los tests,
**Then:**
- Se agrega `tests/unit/calendar/agenda-view.test.ts` con al menos 5 casos usando las funciones puras de calendar-utils:
  1. Días con eventos aparecen en la agenda (usando `getEventsForDay`)
  2. Días sin eventos no aparecen en la lista
  3. Próximos 30 días — día 31 excluido
  4. Estado vacío: `daysWithEvents.length === 0`
  5. Duración formateada correctamente: `formatMinutes` — `30m`, `60m → 1h 0m`, `90m → 1h 30m`
- Todos los tests anteriores pasan sin regresión (343+ → 348+ tests)
- CI: lint 0 errores, typecheck 0 errores, build ✅

## Scope

### IN (esta story)
- `app/(app)/calendar/_components/CalendarClient.tsx` — mejoras a `AgendaView`: duración (AC2) y área/badge (AC3)
- `app/(app)/calendar/_components/CalendarClient.tsx` — exportar `formatMinutes` para uso en tests
- `tests/unit/calendar/agenda-view.test.ts` — nuevo archivo con 5+ tests de comportamiento de Agenda

### OUT (fuera de scope)
- Nueva query de DB para Agenda (ya se usa `getActivitiesForYear` — sin cambio en page.tsx)
- Re-fetch al navegar en Agenda (página carga año completo; navegación filtra en memoria)
- CRUD de eventos desde Agenda (Story 5.7)
- Time tracking start/stop en Agenda (Story 5.8)
- Supabase Realtime (Story 5.9)
- Drill-down a vista Día al clickear un evento de Agenda (Story futura)

## Tasks

- [x] Task 1: Mejorar `AgendaView` en `CalendarClient.tsx` — agregar duración (AC2) y badge de área (AC3): `Xm`/`Xh Ym` junto a hora, `description` como tag si existe, sin regresar "undefined"
- [x] Task 2: Exportar `formatMinutes` de `CalendarClient.tsx` para uso en tests (cambiar de función interna a `export function formatMinutes`)
- [x] Task 3: Crear `tests/unit/calendar/agenda-view.test.ts` con 5+ casos: días con/sin eventos, 30-day window, estado vacío, formatMinutes
- [x] Task 4: Verificar sin regresión — 343 → 353 tests (+10), lint 0 errores, typecheck 0 errores, build ✅

## Dev Notes

### Estado actual de AgendaView (Story 5.5 heredado)

La `AgendaView` en `CalendarClient.tsx` líneas 367-414 ya está implementada y funcional:
- Filtra los próximos 30 días desde `currentDate`
- Agrupa por día con `getEventsForDay`
- Muestra hora (`format(evt.start, 'HH:mm')`) y título
- Muestra color de área con `EVENT_COLOR_CLASSES[evt.color ?? 'blue']`
- Estado vacío: "No hay eventos en los próximos 30 días"

**Solo falta agregar:** duración del evento (AC2) y nombre del área (AC3).

### Patrón de duración a agregar en AgendaView

```typescript
// En la fila de cada evento dentro de AgendaView:
const durationMs = evt.end.getTime() - evt.start.getTime()
const durationMin = Math.round(durationMs / 60000)

// Mostrar junto a hora:
<span className="text-xs text-muted-foreground">
  {format(evt.start, 'HH:mm')} · {formatMinutes(durationMin)}
</span>
```

### Patrón de badge de área a agregar en AgendaView

```typescript
// Después del título — mostrar solo si description existe:
{evt.description && (
  <span className="text-xs text-muted-foreground/70 ml-1">
    {evt.description}
  </span>
)}
```

### Exportar formatMinutes

```typescript
// ANTES: function formatMinutes(minutes: number): string {
// DESPUÉS:
export function formatMinutes(minutes: number): string {
  // misma implementación — solo agregar export
}
```

**Nota:** El comportamiento de `formatMinutes` con valores < 60 retorna `Xh 0m` — para Agenda, filtrar casos < 60 y mostrar solo `Xm` sin la parte de horas. Verificar si se prefiere:
- **Opción A:** `30m` para < 60min, `1h 30m` para >= 60min (más legible en Agenda)
- **Opción B:** Siempre `Xh Ym` (consistente con TimeBudgetPanel)

**Recomendación:** Opción A para Agenda — más natural en una lista de compromisos.

```typescript
export function formatMinutes(minutes: number): string {
  const abs = Math.abs(minutes)
  const h = Math.floor(abs / 60)
  const m = Math.round(abs % 60)
  if (h === 0) return `${m}m`
  return `${h}h ${m}m`
}
```

⚠️ **Este cambio afecta `TimeBudgetPanel` y `WeeklyTimeBudgetPanel`** — verificar que siguen funcionando correctamente. Si `TimeBudgetPanel` espera siempre `Xh Ym`, mantener la función original y crear una versión específica para Agenda.

**Decisión del @dev:** Mantener `formatMinutes` original (`Xh Ym` siempre) para los Time Budget panels y crear función local `formatDuration` en `AgendaView` para mostrar `Xm`/`Xh Ym`.

### Tests — patrón

```typescript
// tests/unit/calendar/agenda-view.test.ts
import { describe, it, expect } from 'vitest'
import { getEventsForDay } from '@/lib/calendar/calendar-utils'
import type { ICalendarEvent } from '@/lib/calendar/calendar-utils'
import { addDays, eachDayOfInterval } from 'date-fns'

// Helper: simula la lógica de AgendaView
function getAgendaDays(currentDate: Date, events: ICalendarEvent[]) {
  const next30Days = eachDayOfInterval({
    start: currentDate,
    end: addDays(currentDate, 29),
  })
  return next30Days
    .map((day) => ({ day, events: getEventsForDay(events, day) }))
    .filter(({ events }) => events.length > 0)
}
```

## Testing

```bash
npm test -- tests/unit/calendar/
npm run lint
npm run typecheck
npm run build
```

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- `getEventsForDay` usa `isSameDay` de date-fns (hora local). Tests de Agenda deben usar `new Date(year, month, day, hour)` (constructor local) en lugar de timestamps UTC — evita timezone drift en CI.
- `formatMinutes` exportada con `export` (compatible con TimeBudgetPanel existente — sigue retornando `Xh Ym`).
- `formatDuration` nueva función interna: `< 60min → Xm`, `60min → Xh`, `> 60min → Xh Ym` — más natural en lista de compromisos.

### Completion Notes

- 343 → 353 tests (+10): `agenda-view.test.ts` nuevo con 10 casos (6 de window logic + 4 de formatDuration/formatMinutes)
- `AgendaView`: duración `HH:mm · Xm/Xh Ym` + badge de área (`evt.description` si existe, `ml-auto` al final de fila)
- `formatMinutes` exportada para tests; `formatDuration` función interna para Agenda (sin regresión en TimeBudgetPanel)
- lint 0 errores, typecheck 0 errores, build ✅ (20.0s, 14/14 páginas)

### File List

```
app/(app)/calendar/_components/CalendarClient.tsx   [MODIFIED — AgendaView duración + área + export formatMinutes]
tests/unit/calendar/agenda-view.test.ts             [NEW — 5+ tests de comportamiento]
docs/stories/5.6.story.md                           [NEW]
```

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Feature Enhancement
**Secondary Type(s):** Frontend Component, Testing
**Complexity:** Low-Medium — mejoras incrementales a AgendaView existente + nuevo test file

### Specialized Agent Assignment

**Primary Agents:**
- @dev (implementación y self-healing)

**Supporting Agents:**
- @qa (quality gate final)

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev
- Max Iterations: 2
- Timeout: 15 min
- Severity Filter: CRITICAL, HIGH
- Behavior CRITICAL: auto_fix (bloquea si persiste tras 2 iteraciones)
- Behavior HIGH: auto_fix (documenta como tech debt si falla tras 2 iteraciones)
- Behavior MEDIUM: document_as_tech_debt
- Behavior LOW: ignore

### Quality Gate Tasks

- [ ] Pre-Commit (@dev) — lint 0 errores, typecheck 0 errores, tests 348+ PASS
- [ ] Pre-PR (@qa) — build ✅, sin regresión en CalendarClient (TimeBudgetPanel, WeeklyTimeBudgetPanel, otras vistas) — QA Gate PASS 7/7

### Focus Areas

**Primary Focus:**
- `formatMinutes` export — verificar que TimeBudgetPanel/WeeklyTimeBudgetPanel no se rompen
- AgendaView: duración correcta (< 60min → `Xm`, >= 60min → `Xh Ym`)
- AgendaView: `description` undefined → no mostrar nada (no "undefined" visible)
- Tests: helper `getAgendaDays` refleja fielmente la lógica de la vista

**Secondary Focus:**
- Regresión: DayView, WeekView, MonthView, YearView sin cambios
- `formatMinutes` con valores negativos (Time Budget overcommitted) — sin regresión
- Tests cubren estado vacío (sin eventos en 30 días)

## QA Results

### QA Gate — Story 5.6 | Fecha: 2026-02-26 | Agente: @qa (Quinn)

#### Veredicto: ✅ PASS

**Puntuación:** 7/7 checks OK · 0 bloqueos · 0 concerns

---

#### 1. Code Review — ✅ PASS

- `AgendaView`: duración calculada como `Math.round((evt.end - evt.start) / 60000)` — correcto, sin overflow.
- `formatDuration`: lógica limpia (`< 60 → Xm`, `= 60 → Xh`, `> 60 → Xh Ym`). Separada de `formatMinutes` para no romper `TimeBudgetPanel`.
- `evt.description && (...)`: condicional correcto — sin "undefined" visible (AC3 cumplido).
- `formatMinutes` exportada con `export` — sin cambio de comportamiento (sigue retornando `Xh Ym` siempre).
- Sin dead code, sin imports no utilizados, sin duplicación.
- Prettier aplicado en pre-commit hook — formato consistente.

#### 2. Unit Tests — ✅ PASS

- **353/353 tests pasando** (343 → +10 nuevos en `agenda-view.test.ts`).
- 10 casos cubiertos: 6 de window logic (incluyendo last day, exclusion day 31, grouping) + 3 de `formatDuration` + 1 de `formatMinutes` regresión.
- Helper `getAgendaDays` en test refleja fielmente la lógica de `AgendaView` (buen patrón).
- Timezone gotcha documentado en Dev Notes: tests usan `new Date(year, month, day, hour)` (constructor local) para evitar drift con `isSameDay`.

#### 3. Acceptance Criteria — ✅ PASS

| AC | Estado | Evidencia |
|----|--------|-----------|
| AC1 — AgendaView datos reales del año | ✅ | Ya hereda `events` de `getActivitiesForYear` (sin cambio en `page.tsx`) |
| AC2 — Duración en cada evento | ✅ | `CalendarClient.tsx` líneas 407-415: `formatDuration(durationMin)` |
| AC3 — Área del evento | ✅ | `CalendarClient.tsx` líneas 418-422: `evt.description &&` condicional |
| AC4 — Navegación por meses | ✅ | Ya implementado: `addMonths`/`subMonths` en `navigate()` |
| AC5 — Tests: 5+ casos | ✅ | `agenda-view.test.ts` 10 casos (supera el mínimo de 5) |

#### 4. Sin Regresiones — ✅ PASS

- **353/353 tests pasan** — 0 regresiones.
- `TimeBudgetPanel` y `WeeklyTimeBudgetPanel` usan `formatMinutes` (ahora exportada pero sin cambio de comportamiento) — sin regresión verificada en `time-budget.test.ts` (9/9 tests).
- DayView, WeekView, MonthView, YearView no modificados.

#### 5. Performance — ✅ PASS

- Sin nueva query de DB — AgendaView filtra del array del año en memoria (correcto para MVP).
- `durationMin` calculado por evento (O(n) eventos por día) — sin overhead.
- Build 20.0s, 14/14 páginas generadas — sin degradación.

#### 6. Seguridad — ✅ PASS

- Sin nueva superficie de ataque: AgendaView solo renderiza datos ya cargados.
- `evt.description` renderizado como texto (no innerHTML) — sin XSS.
- Sin cambios en queries o autenticación.

#### 7. Documentación — ✅ PASS

- Dev Notes documenta timezone gotcha de `isSameDay` (patrón reutilizable).
- `formatDuration` vs `formatMinutes` decision documentada.
- File List completo y preciso.

---

#### CI Summary

```
Tests:      353/353 ✅ (+10)
Lint:       0 errors, 2 warnings (pre-existentes) ✅
Typecheck:  0 errors ✅
Build:      ✅ (20.0s, 14/14 páginas generadas)
```

**Decisión:** Story 5.6 cumple todos los ACs, sin regresiones, con cobertura superior al mínimo. **APROBADA para merge.**

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-02-26 | @sm (River) | Story created from epic-details.md E5 + Story 5.5 context |
| 2026-02-26 | @po (Pax) | Validación 10-point checklist — GO (10/10). Status: Draft → Ready |
| 2026-02-26 | @dev (Dex) | Implemented — 353/353 tests (+10), lint 0 errores, typecheck 0 errores, build ✅. Status: Ready → InReview |
| 2026-02-26 | @qa (Quinn) | QA Gate PASS 7/7 — 353/353 tests, 0 errores lint/typecheck, build ✅, todos los ACs verificados. Status: InReview → Done |
