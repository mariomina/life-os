# Story 1.2: Configuración Supabase (proyecto, Auth magic link/email, RLS base)

## Status

InProgress

## Executor Assignment

```yaml
executor: 'Human + @dev'
quality_gate: '@qa'
quality_gate_tools:
  - docs/architecture/security.md
  - docs/architecture/tech-stack.md
  - docs/architecture/source-tree.md
```

## Story

**As a** developer working on life-os,
**I want** Supabase configured with auth (magic link), Supabase client utilities in the codebase, middleware protecting authenticated routes, a login page, and all DB tables with RLS enabled,
**so that** the app has a secure auth layer and the database is ready for user-scoped data in all subsequent features.

## Dependencies

- **Requires:** Story 1.1 — Status: Done ✅ (Next.js foundation, estructura de directorios, `.env.example`, `.prettierignore` configurados)
- **Blocks:** Story 1.3 (CI/CD necesita proyecto con auth funcional), Story 1.4 (DB schema completo requiere proyecto Supabase existente), E2+ (toda feature autenticada depende de esta story)

## Scope

**IN scope:**
- Creación del proyecto Supabase (paso humano)
- Instalación de `@supabase/supabase-js` y `@supabase/ssr`
- Clientes Supabase server-side y client-side (`lib/supabase/`)
- Middleware de protección de rutas autenticadas
- Login page con magic link
- Auth callback Route Handler
- `supabase/config.toml` para dev local
- Aplicar schema Drizzle existente al proyecto Supabase (`drizzle-kit push`)
- RLS habilitado + policies base en las 15 tablas

**OUT of scope:**
- CI/CD pipeline → Story 1.3 (@devops)
- DB schema refinement o nuevas tablas → Story 1.4 (@data-engineer)
- Testing framework (Vitest) → Story 1.6 (@dev)
- Customización de email templates de Supabase
- Supabase local via Docker (`supabase start`) — usar proyecto cloud para MVP
- OAuth providers (Google, GitHub) — solo magic link + email/password en MVP

## Complexity

**Estimación:** M — 5 story points
- Razón: Auth setup con SSR requiere coordinación entre middleware/server/client; RLS en 15 tablas es repetitivo pero sistemático; paso humano puede bloquear el trabajo de AI

## Risks

| Riesgo | Probabilidad | Impacto | Mitigación |
|--------|-------------|---------|------------|
| Paso humano bloquea AI | Alta | Alto | Documentar pasos humanos claramente en Task 1; AI puede preparar código mientras human configura Supabase |
| `SERVICE_ROLE_KEY` expuesta accidentalmente | Baja | Crítico | CodeRabbit focus área; lint rule; Dev Notes lo documenta explícitamente |
| RLS incompleta (tabla olvidada) | Media | Alto | Script SQL completo en `supabase/rls-policies.sql`; verificación visual en Dashboard |
| Cookie session no refreshada → logout inesperado | Media | Medio | `@supabase/ssr` maneja refresh automático si middleware llama `getUser()` |
| `drizzle-kit push` falla por conflictos | Baja | Medio | Schema es nuevo en DB vacía — no debería haber conflictos; recrear DB si es necesario |

## Acceptance Criteria

1. Proyecto Supabase creado en dashboard — URL y API keys copiadas a `.env.local` (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL)
2. `@supabase/supabase-js` y `@supabase/ssr` instalados — `npm run build` pasa sin errores
3. `lib/supabase/server.ts` creado — exporta `createServerClient()` usando `@supabase/ssr` con cookies httpOnly para Server Components y Server Actions
4. `lib/supabase/client.ts` creado — exporta `createBrowserClient()` usando `@supabase/ssr` para Client Components
5. `middleware.ts` en raíz del proyecto — verifica sesión Supabase en todas las rutas `/(app)/*` y redirect a `/login` si no autenticado; rutas `/(auth)/*` son públicas
6. `app/(auth)/login/page.tsx` creado — formulario de magic link con campo email, botón "Enviar enlace", y mensaje de confirmación post-submit
7. `actions/auth.ts` creado — Server Action `sendMagicLink(email)` que llama a `supabase.auth.signInWithOtp()` y retorna `ActionResult<void>`
8. Configuración magic link en Supabase Auth dashboard — Email confirmations activadas, redirect URL configurada a `http://localhost:3000/auth/callback`
9. `app/(auth)/callback/route.ts` creado — Route Handler que intercambia el code por sesión Supabase y redirect a `/`
10. `supabase/config.toml` creado con configuración básica para dev local (project_id, pool_size, auth settings)
11. Schema Drizzle aplicado a Supabase — `npx drizzle-kit push` ejecuta sin errores, las 15 tablas visibles en Supabase Dashboard → Table Editor
12. RLS habilitado en las 15 tablas del schema — `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + policy base `user_id = auth.uid()` para SELECT/INSERT/UPDATE/DELETE en cada tabla
13. Flujo end-to-end verificado: enviar email → recibir link → click → sesión activa → redirect a `/` → protected por middleware

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: Security (Auth + RLS)
- Secondary Type(s): Backend/API (Server Actions, Route Handlers)
- Complexity: High (auth crítica, cookies httpOnly, middleware, RLS en 15 tablas)

**Specialized Agent Assignment:**
- Primary Agents: @dev (implementación), @architect (revisión de patrones de auth)
- Supporting Agents: @qa (security review post-implementación)

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted` — verificar especialmente: no secrets en código, RLS en todas las tablas, cookies httpOnly
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main` — security scan completo

**CodeRabbit Focus Areas:**
- Primary: Secrets management (SERVICE_ROLE_KEY nunca al cliente, no en logs), RLS coverage (todas las tablas)
- Secondary: Cookie security (httpOnly, secure, sameSite), middleware routing logic

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode, 2 iteraciones)
- CRITICAL: auto_fix
- HIGH: document_only
- MEDIUM: ignore
- LOW: ignore

## Tasks / Subtasks

- [x] **Task 1: Setup Supabase Project (Human)** (AC: 1)
  - [x] 1.1 Crear cuenta/proyecto en [supabase.com](https://supabase.com) — nombre: `life-os`, region: `us-east-1`
  - [x] 1.2 Ir a Project Settings → API → copiar Project URL y anon/public key
  - [x] 1.3 Ir a Project Settings → Database → copiar Connection String (Direct, port 5432)
  - [x] 1.4 Crear `.env.local` en raíz (ya en `.gitignore`) con valores reales

- [x] **Task 2: Instalar dependencias Supabase** (AC: 2)
  - [x] 2.1 Instalar paquetes: `npm install @supabase/supabase-js @supabase/ssr`
  - [x] 2.2 Verificar que `npm run build` pasa sin errores tras instalación

- [x] **Task 3: Crear clientes Supabase** (AC: 3, 4)
  - [x] 3.1 Crear `lib/supabase/server.ts` — `createServerClient` con `@supabase/ssr` y cookies httpOnly
  - [x] 3.2 Crear `lib/supabase/client.ts` — `createBrowserClient` con `@supabase/ssr`
  - [x] 3.3 Verificar TypeScript sin errores en ambos archivos (`npm run typecheck`)

- [x] **Task 4: Crear middleware de auth** (AC: 5)
  - [x] 4.1 Crear `middleware.ts` en raíz — verificar sesión con `createServerClient`
  - [x] 4.2 Proteger rutas `/(app)/*` — redirect a `/login` si `user === null`
  - [x] 4.3 Dejar rutas `/(auth)/*` y `/auth/*` como públicas (sin redirect)
  - [x] 4.4 Actualizar sesión en cada request (refreshSession automático via `@supabase/ssr`)
  - [x] 4.5 Configurar `matcher` en middleware para excluir archivos estáticos y API routes de Next.js

- [x] **Task 5: Crear login page** (AC: 6, 7)
  - [x] 5.1 Crear `actions/auth.ts` con Server Action `sendMagicLink(formData)`
  - [x] 5.2 Crear `app/(auth)/login/page.tsx` con form, confirmación y manejo de errores
  - [ ] 5.3 Verificar que el formulario renderiza correctamente en `localhost:3000/login`

- [x] **Task 6: Crear Auth Callback Route Handler** (AC: 8, 9)
  - [ ] 6.1 Configurar en Supabase Dashboard → Authentication → URL Configuration:
        - Site URL: `http://localhost:3000`
        - Redirect URLs: `http://localhost:3000/auth/callback`
        - ⚠️ Pendiente: requiere Personal Access Token o acción manual en Dashboard
  - [x] 6.2 Activar en Supabase Dashboard → Authentication → Providers → Email:
        - Enable Email provider ✓ — verificado via /auth/v1/settings: email=true
        - Confirm email: ON — mailer_autoconfirm=false (correcto para magic link)
  - [x] 6.3 Crear `app/auth/callback/route.ts` — Route Handler GET (movido de (auth)/callback a auth/callback para URL correcta `/auth/callback`)

- [x] **Task 7: Crear supabase/config.toml** (AC: 10)
  - [x] 7.1 Crear `supabase/config.toml` con auth settings, site_url, redirect_urls

- [x] **Task 8: Aplicar schema Drizzle a Supabase** (AC: 11)
  - [x] 8.1 Verificar que `DATABASE_URL` apunta al proyecto Supabase correcto
  - [x] 8.2 Ejecutar `npx drizzle-kit push --force` — 15 tablas creadas sin errores
  - [x] 8.3 Verificar en Supabase Dashboard → Table Editor que aparecen las 15 tablas — verificado via MCP list_tables (15 tablas, todas con rls_enabled: true)
  - [x] 8.4 Verificar `npm run typecheck` sin errores post-push

- [x] **Task 9: Habilitar RLS en todas las tablas** (AC: 12)
  - [x] 9.1 En Supabase Dashboard → SQL Editor, ejecutar `supabase/rls-policies.sql` — aplicado via MCP migration `rls_policies_story_1_2`
  - [x] 9.2 Script generado con policies `user_id = auth.uid()` para las 14 tablas con user_id directo
  - [x] 9.3 Script generado con policies especiales para `workflow_templates` (system templates visibles)
  - [x] 9.4 Verificar en Supabase Dashboard → Authentication → Policies que aparecen todas las policies — 18 policies verificadas via pg_policies
  - [x] 9.5 Script documentado en `supabase/rls-policies.sql`

- [ ] **Task 10: Verificar flujo end-to-end** (AC: 13)
  - [x] 10.1 Iniciar `npm run dev` — sin errores de consola relacionados con Supabase (✓ Ready in 3.5s)
  - [x] 10.2 Navegar a `localhost:3000` — debe redirect a `/login` — verificado: HTTP 307 → /login
  - [ ] 10.3 Ingresar email real → enviar → recibir email con magic link
  - [ ] 10.4 Click en magic link → redirect a `/auth/callback` → sesión creada → redirect a `/`
  - [ ] 10.5 Verificar que ya no redirecta a `/login` — sesión activa
  - [x] 10.6 Ejecutar `npm run build` + `npm run typecheck` + `npm run lint` — todo pasa (0 errores, 9 warnings en script utilitario)

## Dev Notes

### Previous Story Insights (Story 1.1)

- `lib/supabase/` directorio ya existe (`.gitkeep`) — reemplazar con archivos reales
- `.env.example` ya tiene las variables Supabase documentadas (sin valores reales)
- `lib/db/client.ts` ya usa `DATABASE_URL` para Drizzle — esta story proporciona los valores reales
- `.env.local` debe estar en `.gitignore` — ya configurado en Story 1.1
- `app/(auth)/layout.tsx` ya existe como layout minimalista (sin sidebar) — NO modificar
- `app/(auth)/login/` directorio existe con `.gitkeep` — reemplazar con `page.tsx`

### Paquetes a instalar

[Source: docs/architecture/tech-stack.md#3.2]

```bash
npm install @supabase/supabase-js @supabase/ssr
```

- `@supabase/supabase-js` — cliente oficial Supabase
- `@supabase/ssr` — helpers para Server Components y middleware Next.js (maneja cookies httpOnly correctamente)

**CRÍTICO:** No usar `@supabase/auth-helpers-nextjs` (deprecado). El paquete correcto es `@supabase/ssr`.

### lib/supabase/server.ts — Implementación

[Source: docs/architecture/security.md#14.2]

```typescript
// lib/supabase/server.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createSupabaseServerClient() {
  const cookieStore = await cookies()

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // setAll llamado desde Server Component — ignorar si el middleware ya maneja cookies
          }
        },
      },
    }
  )
}
```

**Uso en Server Actions y Server Components:**
```typescript
import { createSupabaseServerClient } from '@/lib/supabase/server'
const supabase = await createSupabaseServerClient()
const { data: { user } } = await supabase.auth.getUser()
```

### lib/supabase/client.ts — Implementación

[Source: docs/architecture/security.md#14.2]

```typescript
// lib/supabase/client.ts
'use client'

import { createBrowserClient } from '@supabase/ssr'

export function createSupabaseBrowserClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

**Uso en Client Components:**
```typescript
import { createSupabaseBrowserClient } from '@/lib/supabase/client'
const supabase = createSupabaseBrowserClient()
```

### middleware.ts — Implementación

[Source: docs/architecture/security.md#14.2]

El middleware protege todas las rutas `/(app)/*`. Las rutas `/(auth)/*` son públicas.

```typescript
// middleware.ts (raíz del proyecto)
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll() },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value))
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  // CRÍTICO: Siempre llamar getUser() — refresca sesión automáticamente
  const { data: { user } } = await supabase.auth.getUser()

  // Si no hay sesión y la ruta es protegida → redirect a /login
  if (!user && !request.nextUrl.pathname.startsWith('/login') &&
      !request.nextUrl.pathname.startsWith('/auth')) {
    const url = request.nextUrl.clone()
    url.pathname = '/login'
    return NextResponse.redirect(url)
  }

  return supabaseResponse
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```

### actions/auth.ts — Implementación

[Source: docs/architecture/coding-standards.md#12.3, docs/architecture/security.md#14.1]

```typescript
// actions/auth.ts
'use server'

import { z } from 'zod'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import type { ActionResult } from '@/types/domain'

const emailSchema = z.string().email('Email inválido')

export async function sendMagicLink(formData: FormData): Promise<ActionResult<void>> {
  const email = formData.get('email') as string

  const parsed = emailSchema.safeParse(email)
  if (!parsed.success) {
    return { success: false, error: parsed.error.errors[0].message }
  }

  try {
    const supabase = await createSupabaseServerClient()
    const { error } = await supabase.auth.signInWithOtp({
      email: parsed.data,
      options: {
        emailRedirectTo: `${process.env.NEXT_PUBLIC_APP_URL}/auth/callback`,
      },
    })

    if (error) {
      return { success: false, error: error.message }
    }

    return { success: true, data: undefined }
  } catch (error) {
    return { success: false, error: 'Error al enviar el magic link' }
  }
}
```

**NOTA:** Zod ya está disponible vía `@supabase/supabase-js` peer dep, pero verificar que esté en dependencies. Si no: `npm install zod`.

### app/(auth)/callback/route.ts — Implementación

[Source: docs/architecture/security.md#14.2]

```typescript
// app/(auth)/callback/route.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')

  if (code) {
    const cookieStore = await cookies()
    const supabase = createServerClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
      {
        cookies: {
          getAll() { return cookieStore.getAll() },
          setAll(cookiesToSet) {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          },
        },
      }
    )
    const { error } = await supabase.auth.exchangeCodeForSession(code)
    if (!error) {
      return NextResponse.redirect(`${origin}/`)
    }
  }

  return NextResponse.redirect(`${origin}/login?error=auth_failed`)
}
```

### RLS Policies — Estrategia

[Source: docs/architecture/security.md#14.2]

**Regla universal:** Todas las tablas tienen `user_id UUID` que referencia `auth.users(id)`. La policy base es:

```sql
CREATE POLICY "user_owns_row" ON {table}
  FOR ALL
  USING (user_id = auth.uid())
  WITH CHECK (user_id = auth.uid());
```

**Tablas con user_id DIRECTO** (policy simple):
- `areas`, `okrs`, `habits`, `skills`, `projects`, `inbox_items`, `correlations`

**Tablas con user_id INDIRECTO** (join requerido):
- `area_scores` → `areas(user_id = auth.uid())`
- `workflow_templates` → `user_id` directo (templates del usuario)
- `workflows` → join `projects(user_id = auth.uid())`
- `tasks` → join `workflows` → `projects` → `user_id`
- `steps_activities` → join `tasks` → `workflows` → `projects` → `user_id`
- `time_entries` → join `steps_activities` → cadena
- `checkin_responses` → join `steps_activities` → cadena
- `step_skill_tags` → join `steps_activities` → cadena

**Archivo a crear:** `supabase/rls-policies.sql` — script completo con todos los ALTER TABLE + CREATE POLICY para poder reproducir el setup.

### Schema existente — referencia de tablas

[Source: lib/db/schema/ — pre-existente, creado por @data-engineer]

Las 15 tablas en `lib/db/schema/`:
1. `areas` — areas de vida (user_id directo)
2. `area_scores` — scores históricos por área
3. `okrs` — Objectives & Key Results (user_id directo)
4. `habits` — hábitos recurrentes (user_id directo)
5. `skills` — habilidades (user_id directo)
6. `workflow_templates` — templates de workflow (user_id directo)
7. `projects` — proyectos (user_id directo)
8. `workflows` — workflows de un proyecto
9. `tasks` — tasks dentro de un workflow
10. `steps_activities` — steps/activities (corazón del sistema)
11. `time_entries` — time tracking
12. `inbox_items` — items de inbox (user_id directo)
13. `checkin_responses` — respuestas de daily check-in
14. `step_skill_tags` — tags de skills en steps
15. `correlations` — resultados del motor de correlaciones (user_id directo)

### Variables de entorno — .env.local (completar en Task 1)

[Source: docs/architecture/infrastructure.md#10.5]

```bash
# Supabase — obtener desde Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://[project-ref].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=[anon-public-key]
SUPABASE_SERVICE_ROLE_KEY=[service-role-key]  # SOLO server-side, NUNCA al cliente

# Database — Transaction Pooler (port 6543) — requerido para Drizzle con serverless
DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**CRÍTICO de seguridad:**
- `SUPABASE_SERVICE_ROLE_KEY` bypasa RLS — NUNCA usar en Client Components
- Solo usar service role key en operaciones de admin server-side (si las hay)
- Para esta story, solo el Server Action y Route Handler usan el cliente servidor

### drizzle.config.ts — verificar antes de push

[Source: drizzle.config.ts existente]

Verificar que `drizzle.config.ts` usa `DATABASE_URL` correcto. El push creará las tablas en el schema `public` de Supabase.

```bash
npx drizzle-kit push  # Aplica schema sin migración file — OK para setup inicial
```

**IMPORTANTE:** Para dev, se puede usar `push` directamente. Para producción, usar `generate` + `migrate`.

### Testing Requirements

[Source: docs/architecture/test-strategy.md#13.1]

Esta story NO requiere tests unitarios. La verificación es end-to-end:
- Login flow manual (enviar magic link, recibirlo, hacer click)
- Middleware test: acceder a ruta protegida sin sesión → verificar redirect
- `npm run build` + `npm run typecheck` + `npm run lint` deben pasar

### Archivos a NO modificar en esta story

- `lib/db/schema/**` — ya existe, no tocar
- `lib/db/client.ts` — ya configurado con Drizzle
- `drizzle.config.ts` — ya configurado
- `app/(auth)/layout.tsx` — ya existe, no modificar
- `components/layout/**` — ya existe desde Story 1.1

## Change Log

| Fecha      | Versión | Descripción                                                                    | Autor      |
| ---------- | ------- | ------------------------------------------------------------------------------ | ---------- |
| 2026-02-20 | 0.1     | Historia inicial creada                                                        | River (@sm) |
| 2026-02-20 | 0.2     | Validación @po — GO (7.5/10) — Añadidas Dependencies, Scope, Complexity, Risks — Status: Draft → Ready | Pax (@po)  |
| 2026-02-21 | 0.3     | Implementación @dev — Tasks 1-8 completas. Bug fix: callback route movida de (auth)/callback a auth/callback para URL /auth/callback correcta. drizzle-kit push exitoso (15 tablas). Status: Ready → InProgress | Dex (@dev) |
| 2026-02-21 | 0.4     | Task 9 completa — RLS aplicado via MCP Supabase (migration rls_policies_story_1_2): 18 policies en 15 tablas. Task 8.3 verificada: 15 tablas con rls_enabled. Build/typecheck/lint pasan. Deuda técnica: middleware.ts deprecado en Next.js 16. | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- `.next/` cache obsoleto apuntaba a `(auth)/callback/route.js` (ruta eliminada) → resuelto con `rm -rf .next` antes de typecheck
- Next.js 16: `middleware.ts` deprecado → debe renombrarse a `proxy.ts` (ver deuda técnica)

### Completion Notes List

- RLS aplicado via Supabase MCP migration `rls_policies_story_1_2` — 18 policies creadas (14 tablas con `user_owns_row` ALL + 4 policies granulares en `workflow_templates`)
- Las 15 tablas verificadas en Supabase con `rls_enabled: true`
- Build, typecheck y lint pasan sin errores
- **DEUDA TÉCNICA:** Next.js 16 deprecó `middleware.ts` en favor de `proxy.ts`. Warning en build: "The 'middleware' file convention is deprecated. Please use 'proxy' instead." → Renombrar en Story futura antes de upgrade mayor
- Tasks 10.1, 10.2 verificadas: servidor dev OK, redirect middleware activo (307 → /login)
- Task 6.2 verificada: email provider activo y mailer_autoconfirm=false (magic link correcto)
- Task 6.1 pendiente: configurar Site URL y Redirect URL en Supabase Dashboard (requiere acción humana)
- Tasks 5.3, 10.3-10.5 pendientes: requieren email real y flujo interactivo en browser

### File List

**Nuevos (creados):**
- lib/supabase/server.ts
- lib/supabase/client.ts
- middleware.ts
- app/(auth)/login/page.tsx
- app/auth/callback/route.ts  ← movido de (auth)/callback para URL correcta `/auth/callback`
- actions/auth.ts
- supabase/config.toml
- supabase/rls-policies.sql
- .env.local (gitignored — valores reales del proyecto Supabase)

**Eliminados:**
- app/(auth)/callback/route.ts (reemplazado por app/auth/callback/route.ts)
- lib/supabase/.gitkeep
- app/(auth)/login/.gitkeep

## QA Results

_Pendiente — ejecutar @qa tras completar implementación_
