# Story 3.1: Vista Áreas — scores D-Needs/B-Needs, tendencia y tiempo invertido

## Status

Done

## Executor Assignment

```yaml
executor: '@dev'
quality_gate: '@qa'
quality_gate_tools:
  - docs/architecture/test-strategy.md
  - docs/architecture/source-tree.md
  - docs/prd/technical-assumptions.md
```

## Story

**As a** usuario con diagnóstico completado,
**I want** una Vista Áreas dedicada en `/areas` con mis 8 áreas Maslow, su score actual, tendencia histórica y tiempo invertido en cada una,
**so that** pueda gestionar y hacer seguimiento de mis áreas de vida desde una vista específica más detallada que el Home.

## Dependencies

- **Requires:** Story 2.5 — Done ✅ (Vista Perfil como base — queries `getUserAreas` y `getRecentAreaScores` ya creadas)
- **Requires:** Story 1.5 — Done ✅ (layout con sidebar/navbar funcional)
- **Blocks:** Story 3.2 (Alertas — requiere esta vista como base de navegación)

## Scope

**IN scope:**
- Página `/areas` — Server Component con Vista Áreas completa
- Reusar `getUserAreas` y `getRecentAreaScores` de Story 2.5
- Calcular tiempo invertido por área desde `time_entries` + `steps_activities` (si no hay datos → 0h)
- Mostrar las 8 áreas agrupadas en D-Needs (1-4) y B-Needs (5-8)
- Card por área: ícono, nombre, score %, barra de progreso coloreada, tendencia (↑ ↓ →), tiempo invertido (hh)
- Score global Life System Health Score en header de la página
- `last_activity_at` por área — mostrar "hace X días" o "Sin actividad"
- Sin diagnóstico → CTA al onboarding (mismo patrón que Home)
- Server Component, TypeScript strict, sin `any`
- Tests unitarios: función `formatTimeInvested(seconds: number): string` — formatea segundos en "Xh Ym"

**OUT of scope:**
- Editar nombre/configuración de áreas (Story futura)
- Gráfico histórico de tendencia (Story 3.x o E8)
- Alertas de área crítica (Story 3.2)
- Drill-down a detalle de área individual
- Timer activo desde esta vista (E5)

## Complexity

**Estimación:** S — 3 story points
- Razón: Reutiliza queries y componentes de Story 2.5. La única lógica nueva es la query de tiempo invertido (join `steps_activities` + `time_entries`) y el formatter de tiempo. Server Component puro.

## Risks

| Riesgo | Probabilidad | Impacto | Mitigación |
|--------|-------------|---------|------------|
| `time_entries` vacía (usuario sin tracking) | Alta | Bajo | Mostrar "0h" — no crash |
| N+1 queries (1 por área para tiempo) | Media | Bajo | 1 sola query GROUP BY area_id |
| `steps_activities` no tiene datos aún (E4 pendiente) | Alta | Bajo | Query LEFT JOIN — devuelve 0 si no hay datos |

## Acceptance Criteria

1. La ruta `/areas` muestra la Vista Áreas para usuarios con diagnóstico completado
2. Se muestran las 8 áreas agrupadas en D-Needs (1-4) y B-Needs (5-8)
3. Cada área muestra: ícono, nombre, score %, barra de progreso coloreada, tendencia (↑ ↓ →)
4. Cada área muestra tiempo invertido total acumulado (desde `time_entries`) en formato "Xh Ym"
5. Cada área muestra `last_activity_at` como "hace X días" o "Sin actividad"
6. El Life System Health Score global aparece en el header de la página
7. Usuarios sin diagnóstico ven CTA "Completar diagnóstico" → `/onboarding`
8. La página es un Server Component — datos fetched en servidor, sin spinner inicial
9. `npm run typecheck` PASS, `npm run lint` PASS (0 errores)
10. Tests unitarios PASS: `formatTimeInvested(seconds: number): string`

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation vía manual review + Vitest tests.

## Tasks / Subtasks

- [x] **Task 1: Query de tiempo invertido por área** (AC: 4)
  - [x] 1.1 Crear `lib/db/queries/time-entries.ts` — `getTimeInvestedByArea(userId)`: suma `duration_seconds` de `time_entries` agrupada por `area_id` via `steps_activities`
  - [x] 1.2 Manejar caso sin datos (INNER JOIN → map vacío, caller usa `?? 0`)

- [x] **Task 2: Utilidad de formato de tiempo** (AC: 4, 10)
  - [x] 2.1 Crear `lib/utils/time-format.ts` — `formatTimeInvested` + `formatLastActivity`
  - [x] 2.2 Tests unitarios: 10 tests — 0s, negativos, minutos, horas exactas, horas+minutos, null, hoy, ayer, hace N días

- [x] **Task 3: UI — Vista Áreas** (AC: 1, 2, 3, 4, 5, 6, 7)
  - [x] 3.1 Crear `app/(app)/areas/page.tsx` como Server Component
  - [x] 3.2 Fetch paralelo: `getUserAreas` + `getRecentAreaScores` + `getTimeInvestedByArea`
  - [x] 3.3 Header: Life System Health Score global
  - [x] 3.4 Sección D-Needs: 4 cards con score, barra, tendencia, tiempo, last_activity_at
  - [x] 3.5 Sección B-Needs: 4 cards con score, barra, tendencia, tiempo, last_activity_at
  - [x] 3.6 Estado vacío: CTA cuando usuario sin áreas

- [x] **Task 4: Verificación final** (AC: 9, 10)
  - [x] 4.1 `npm test` → 61/61 tests PASS
  - [x] 4.2 `npm run typecheck` → 0 errores
  - [x] 4.3 `npm run lint` → 0 errores (9 warnings preexistentes en scripts/)
  - [x] 4.4 `npm run build` → PASS — `/areas` como Dynamic Server Component

## Dev Notes

### Query de tiempo invertido por área

```typescript
// lib/db/queries/time-entries.ts
import { eq, sum, sql } from 'drizzle-orm'
import { db } from '@/lib/db/client'
import { timeEntries } from '@/lib/db/schema'
import { stepsActivities } from '@/lib/db/schema'

export async function getTimeInvestedByArea(
  userId: string
): Promise<Record<string, number>> {
  const rows = await db
    .select({
      areaId: stepsActivities.areaId,
      totalSeconds: sum(timeEntries.durationSeconds).mapWith(Number),
    })
    .from(timeEntries)
    .innerJoin(stepsActivities, eq(timeEntries.stepActivityId, stepsActivities.id))
    .where(eq(timeEntries.userId, userId))
    .groupBy(stepsActivities.areaId)

  const map: Record<string, number> = {}
  for (const row of rows) {
    if (row.areaId) map[row.areaId] = row.totalSeconds ?? 0
  }
  return map
}
```

### Formato de tiempo

```typescript
// lib/utils/time-format.ts
export function formatTimeInvested(seconds: number): string {
  if (seconds <= 0) return '0h'
  const h = Math.floor(seconds / 3600)
  const m = Math.floor((seconds % 3600) / 60)
  if (h === 0) return `${m}m`
  if (m === 0) return `${h}h`
  return `${h}h ${m}m`
}
```

### last_activity_at — "hace X días"

```typescript
function formatLastActivity(lastActivityAt: Date | null): string {
  if (!lastActivityAt) return 'Sin actividad'
  const diffDays = Math.floor(
    (Date.now() - new Date(lastActivityAt).getTime()) / (1000 * 60 * 60 * 24)
  )
  if (diffDays === 0) return 'Hoy'
  if (diffDays === 1) return 'Ayer'
  return `hace ${diffDays} días`
}
```

### Server Component pattern

```typescript
// app/(app)/areas/page.tsx — Server Component (no 'use client')
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { getUserAreas } from '@/lib/db/queries/areas'
import { getRecentAreaScores } from '@/lib/db/queries/area-scores'
import { getTimeInvestedByArea } from '@/lib/db/queries/time-entries'
import { calculateGlobalScore } from '@/features/maslow/scoring'
import { calculateTrend, scoreColorClass, scoreBgClass } from '@/lib/utils/trend'
import { formatTimeInvested } from '@/lib/utils/time-format'

export default async function AreasPage() {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const [userAreas, recentScores, timeMap] = await Promise.all([
    getUserAreas(user.id),
    getRecentAreaScores(user.id, 7),
    getTimeInvestedByArea(user.id),
  ])
  // ...
}
```

### Schema relevante

- `time_entries`: `step_activity_id`, `user_id`, `duration_seconds`, `started_at`, `ended_at`, `is_active`
- `steps_activities`: `id`, `area_id`, `user_id`
- Reusar `areas` + `area_scores` (ya en queries existentes de Story 2.5)

## Change Log

| Fecha      | Versión | Descripción                              | Autor        |
| ---------- | ------- | ---------------------------------------- | ------------ |
| 2026-02-22 | 0.1     | Historia creada — Status: Draft          | River (@sm)  |
| 2026-02-22 | 0.2     | Validación @po — GO (10/10) — Status: Draft → Ready | Pax (@po) |
| 2026-02-22 | 0.3     | Implementación completa — 61 tests PASS, build PASS — Status: Ready for Review | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- Sin bloqueadores relevantes

### Completion Notes List

- `getTimeInvestedByArea`: INNER JOIN `time_entries` → `steps_activities` GROUP BY `area_id`, query única sin N+1
- `formatTimeInvested`: lógica pura "0h"/"Xm"/"Xh"/"Xh Ym" + `formatLastActivity` relativo
- 10 tests unitarios en `time-format.test.ts` — todos los casos borde cubiertos
- Server Component `app/(app)/areas/page.tsx`: fetch paralelo con `Promise.all` (3 queries)
- AreaCard extendida: añade ⏱ tiempo invertido + "hace X días / Sin actividad"
- EmptyState idéntico al Home — CTA → `/onboarding`
- 61/61 tests PASS, typecheck 0 errores, lint 0 errores, build PASS

### File List

**Nuevos:**
- `lib/db/queries/time-entries.ts`
- `lib/utils/time-format.ts`
- `tests/unit/utils/time-format.test.ts`
- `app/(app)/areas/page.tsx`

**Modificados:**
- _(ninguno esperado)_

## QA Results

### Gate Decision: PASS (99/100)

**Fecha:** 2026-02-22
**Revisor:** Quinn (@qa)

#### ACs Verificados: 10/10

| AC | Descripción | Estado |
|----|-------------|--------|
| 1 | `/areas` muestra Vista Áreas con diagnóstico completado | ✅ PASS |
| 2 | 8 áreas agrupadas D-Needs + B-Needs | ✅ PASS |
| 3 | Ícono, nombre, score %, barra coloreada, tendencia | ✅ PASS |
| 4 | Tiempo invertido "Xh Ym" desde `time_entries` | ✅ PASS |
| 5 | `last_activity_at` relativo ("hace X días" / "Sin actividad") | ✅ PASS |
| 6 | Life System Health Score en header | ✅ PASS |
| 7 | EmptyState CTA → `/onboarding` | ✅ PASS |
| 8 | Server Component — sin spinner inicial | ✅ PASS |
| 9 | typecheck PASS, lint PASS (0 errores) | ✅ PASS |
| 10 | 61/61 tests PASS — `formatTimeInvested` + `formatLastActivity` | ✅ PASS |

#### Observaciones

- **[LOW]** Sin tests de integración para `getTimeInvestedByArea` — fuera de scope MVP. -1pt.
- Sin issues CRITICAL o HIGH.

#### Tests

- 61/61 PASS: time-format (10), trend/utils (10), ai/upload-analysis (5), maslow/questionnaire (12), actions/questionnaire (4), maslow/scoring (9), utils/maslow-weights (4), actions/onboarding (7)
- Build: PASS — `/areas` como Dynamic Server Component

**Decisión final: Story 3.1 → Done ✅**
_Pendiente_
